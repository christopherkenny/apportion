template_var_size <- function() { # nocov start
  'an integer number of seats to apportion across units'
} # nocov end

template_var_pop <- function() { # nocov start
  'a vector of population sizes for each unit'
} # nocov end

template_var_return <- function() { # nocov start
  'an integer vector of the same length as `pop` with the number of seats apportioned to each unit.'
} # nocov end
